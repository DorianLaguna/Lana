/// Punto de entrada del paquete `LanaSpeech`: transcripción de voz on-device
/// (ADR-0015). Implementa `SpeechTranscribing` de `LanaCore` con el Speech
/// framework de Apple — ninguna feature lo importa directamente.
public enum LanaSpeech {
    /// Nombre del módulo.
    public static let moduleName = "LanaSpeech"
}
