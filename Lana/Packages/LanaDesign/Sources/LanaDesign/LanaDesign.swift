/// Punto de entrada del paquete `LanaDesign`. Tokens, temas y componentes
/// reales se agregan en la Fase 4 — ver PLAN.md sección 4.
public enum LanaDesign {
    /// Nombre del módulo. Existe para que el scaffold de Fase 0 tenga algo
    /// concreto que probar sin inventar tipos de diseño antes de tiempo.
    public static let moduleName = "LanaDesign"
}
