/// Punto de entrada del paquete `OnboardingFeature` — el flujo de onboarding
/// de primer arranque y la guía de configuración de Apple Pay (ADR-0009).
public enum OnboardingFeature {
    /// Nombre del módulo.
    public static let moduleName = "OnboardingFeature"
}
