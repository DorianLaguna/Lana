/// Punto de entrada del paquete `EntryFeature`. La vista y el modelo reales se agregan
/// en una fase posterior — ver PLAN.md sección 4.
public enum EntryFeature {
    /// Nombre del módulo. Existe para que el scaffold de Fase 0 tenga algo
    /// concreto que probar sin inventar código real antes de tiempo.
    public static let moduleName = "EntryFeature"
}
