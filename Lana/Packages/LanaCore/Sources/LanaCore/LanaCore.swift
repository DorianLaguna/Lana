/// Punto de entrada del paquete `LanaCore`. El dominio real (`Money`, `Ledger`,
/// eventos, protocolos) se agrega en la Fase 1 — ver PLAN.md sección 4.
public enum LanaCore {
    /// Nombre del módulo. Existe para que el scaffold de Fase 0 tenga algo
    /// concreto que probar sin inventar tipos de dominio antes de tiempo.
    public static let moduleName = "LanaCore"
}
