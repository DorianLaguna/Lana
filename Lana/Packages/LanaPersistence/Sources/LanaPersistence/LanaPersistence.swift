/// Punto de entrada del paquete `LanaPersistence`. La implementación real se agrega en
/// una fase posterior — ver PLAN.md sección 4.
public enum LanaPersistence {
    /// Nombre del módulo. Existe para que el scaffold de Fase 0 tenga algo
    /// concreto que probar sin inventar código real antes de tiempo.
    public static let moduleName = "LanaPersistence"
}
